(()=>{const $=s=>document.querySelector(s),key='ordergrid-rewards';let ledger={received:0,redeemed:0};try{ledger=Object.assign(ledger,JSON.parse(localStorage.getItem(key))||{})}catch{}const save=()=>localStorage.setItem(key,JSON.stringify(ledger));function earned(){try{return(JSON.parse(localStorage.getItem('ordergrid-demo'))?.settlements||[]).reduce((n,s)=>n+(+s.rewards||0),0)}catch{return 0}}function render(){const total=earned(),available=Math.max(0,total-ledger.received-ledger.redeemed);$('#totalSuperCoins').textContent=total;$('#availableSuperCoins').textContent=available;$('#receivedSuperCoins').textContent=ledger.received;$('#redeemedSuperCoins').textContent=ledger.redeemed;$('#receiveRewards').disabled=!available;document.querySelectorAll('[data-redeem]').forEach(b=>b.disabled=(ledger.received-ledger.redeemed)<+b.dataset.redeem)}$('#receiveRewards').onclick=()=>{const available=Math.max(0,earned()-ledger.received-ledger.redeemed);ledger.received+=available;save();render();$('#toast').textContent=`${available} SuperCoins received in rewards balance`;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),2000)};document.querySelector('.offer-grid').onclick=e=>{const b=e.target.closest('[data-redeem]');if(!b)return;const cost=+b.dataset.redeem;if(ledger.received-ledger.redeemed<cost)return;ledger.redeemed+=cost;save();render();$('#toast').textContent='Offer redeemed and added to benefits';$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),2000)};window.addEventListener('ordergrid:update',render);render()})();
